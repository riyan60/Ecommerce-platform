#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../node-gyp-build/optional.js" "$@"
else
  exec node  "$basedir/../../../node-gyp-build/optional.js" "$@"
fi
# cmd-shim-target=C:/Users/riyan/OneDrive/Desktop/Projects/Ecommerce-platform/node_modules/.pnpm/bcrypt@6.0.0/node_modules/node-gyp-build/optional.js
